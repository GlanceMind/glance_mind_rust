# Handoff: Step 02 → Step 03 (scope split / module map)

## Next Step

`03-split` — 把 R001–R010 切成模块计划队列。manifest 已有候选 M1–M6；Step 03 需确认边界、依赖序（建议 M5 迁移与 M1–M4 的先后、M2 校验函数被 M3/M4 复用的依赖）、每模块大小适配单次上下文。

## Required Reads (only these)

1. `00-manifest.md`
2. `01-first-principles.md`
3. `ledgers/requirements.md`
4. `ledgers/invariants-failures.md`
5. `ledgers/test-suite.md`
6. `~/.claude/skills/jplan/references/step-03-split.md`
7. （路径查找用）`00-context-inventory.md`

## Avoid Reading

- 其余 ledgers（anti-gaming/production-deps/providers/llm/framework/non-code）——Step 04 起草时按模块取用。
- 源代码文件——split 只需 inventory 的路径。

## Open Items for Step 03

- M2 的校验函数放哪个 crate/模块（service 层公共 util vs repository 查询）——影响 M3/M4 依赖描述。
- M5 与 M2/M3/M4 上线顺序已定（D2：同 PR 先后皆可，迁移先跑）；split 时标注 M5 为 M3/M4 集成测试的前置（IT 需要干净数据语义）。

## Validation Evidence (Step 02)

- 9 个 ledger 全部写入；R001–R010 每条都有 acceptance evidence 与 FP 引用，无 origin: assumed。
- 每特性有确定性测试（UT/IT 表）；真实生产依赖（Postgres）有真依赖测试与演练要求。
- provider/LLM 两门 INAPPLICABLE 且附逐改动面证据与作废条件。
- 核心模块变异阈值（≥80%，cargo-mutants）与属性测试（proptest 三性质）已命名。
- D1/D2/D3 三个 open decision 全部落锤（requirements.md 决策节）。
