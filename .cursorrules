# glance_mind_rust Cursor Context Pointer

Keep startup context small.

- Use `/Users/jacksoom/programer/aihub/.cursor/skills/glancemind-context-router` to choose project-specific context.
- Load workflow, architecture, and domain guards only when their trigger is directly present.
- Do not duplicate MCP transport contracts or shared GlanceMind architecture here.
- Preserve user changes and avoid destructive git commands unless explicitly approved.
