[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    requires_llm: Tests requiring LLM (skipped in bare API mode, run in E2E with mock)
addopts = -v --tb=short --strict-markers
