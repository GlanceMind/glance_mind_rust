[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    aipub: AIPub plan full-pipeline tests
    campaign: Campaign crawler full-pipeline tests
    requires_llm: Tests that require LLM service
addopts = -v --tb=short -x --strict-markers
